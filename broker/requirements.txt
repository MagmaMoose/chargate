# Runtime deps for the Chargate token-broker service (broker/), used by the
# container image. Kept in sync with the `broker` dependency-group in pyproject.toml.
# These are NOT dependencies of the chargate CLI, which stays stdlib-only.
fastapi>=0.115
uvicorn[standard]>=0.30
pyjwt[crypto]>=2.9
httpx>=0.27
pydantic-settings>=2.5
