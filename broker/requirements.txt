# Runtime deps for the Chargate token-broker service (broker/), used by the
# container image. These are NOT dependencies of the chargate CLI, which stays
# stdlib-only.
#
# Exact pins, deliberately: pyproject.toml's `broker` dependency-group declares the
# supported *ranges* (abstract deps), while this file is the *concrete*, resolved
# set baked into the image — so a rebuild of an old tag reproduces byte-for-byte
# and hadolint DL3013 is satisfied. Regenerate after changing the group with:
#   uv export --group broker --no-hashes --no-emit-project
fastapi==0.138.0
uvicorn[standard]==0.49.0
pyjwt[crypto]==2.13.0
httpx==0.28.1
pydantic-settings==2.14.2
